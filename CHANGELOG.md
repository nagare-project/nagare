# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 本地来源插件与自动换源

- 完成与 Nagare Source 最终版 Plugin API v1 的兼容：来源健康状态按 `healthy`、`degraded`、`unavailable`、`interactive_required` 和 `disabled` 校验并显示，避免可用来源因旧状态名被错误标灰；只有 `.torrent` 地址的候选也会经过有界下载和内网地址拦截后进入既有 BT 播放管线
- 用户可以在设置中显式启用本地来源插件并选择绝对可执行文件与仓库目录；Nagare 启动子进程，通过有界单行 readiness 发现系统分配的回环端口，协商 Plugin API v1，并在禁用、重新配置或退出时终止插件
- 插件连接只接受显式回环 HTTP 地址，不跟随重定向；manifest、来源和 Candidate 事件均做严格边界校验。来源状态与失败分类在界面可见，用户可以取消仍在进行的搜索
- 作品页增量展示在线与 BT 候选。可靠的高优先级在线候选可在搜索完成前立即起播；同步启动失败或 mpv 异步播放错误会尝试下一在线候选，在线耗尽后回退到既有 BT 播放管线，并保留手动换源与重试入口
- 正常播完、用户停止或手动换源不会触发自动回退。短期媒体 URL、Cookie 和请求头只存在于当前播放会话，候选流禁止缓存，播放器状态、界面与普通日志不显示这些敏感值

### 目录与账号后端

- Discover、作品详情与放送表统一通过 animego 获取元数据，移除直连 AniList；六个板块独立降级，目录缓存十分钟，放送缓存三十分钟
- Schedule 改用真实放送时间并按本机时区展示；上游只提供近七日，不再展示演示缺集信息
- 收藏按账号隔离缓存一分钟；只保留四种状态、评分与进度，删除本机日期、重看和搁置字段。退进度按实际已看集合降序撤销，中途失败明确报告已完成操作
- 图片按服务端登记键读取，登记表限制 4096 条并按最近使用淘汰，继续复用既有 HTTPS 和内网访问限制
- 预告片元数据由 animego 提供，兼容季度列表和详情字段；旧列表缺少字段时悬停按需补全，保留现有 YouTube 播放效果。上游字段需要部署配套迁移后生效
- 磁力种子只有一个视频但与目标集号不符或无法确定时，也要求手动选择

### 界面

- 作品预览、Discover 悬停卡片与作品页增加磁力选集入口：按已播出集数选择目标集，使用用户配置的本机规则搜索并显式选择资源；播放状态跨页面共享，种子内选集与重试都会保留目标集号
- 修复新开标签页或重开浏览器后提示“缺少或错误的鉴权 token”：本机凭证按同源持久保存，兼容旧标签页并优先使用新启动凭证
- Discover 接入实时 animego 榜单、本地类型筛选与作品详情；图片经本机缓存代理加载，失败时提供重试
- 点击封面或标题进入独立作品页，右键可打开预览；详情补齐英文副标题、制作公司、排名、关联作品、推荐与角色
- 收藏列表与编辑接入 animego 账号，支持状态、评分与进度同步；仅保存上游支持的四种状态、评分与进度
- 修正英文标题空格被计入动画延迟的问题，使逐词淡入与原版时序一致
- 修正真实长标题的两行高度、轮播行间距、放送徽标、最近播出时间、下一集倒计时、评分配色和作品页横图裁切

- Discover 再次按原版实测对齐：顶部文字导航、30px 分区标题、40px 分类控件、轮播裁切边界、
  行间距与悬停层评分排版；类型栏补齐渐变箭头、惯性拖拽和滑动选中背景
- 作品预览改为宽幅详情窗口，使用大封面与渐变横图，并可打开独立预告片播放器；关闭预告片或
  本地选集后返回外层预览，关闭时停止视频并恢复焦点
- Discover 悬停卡片主按钮改为播放图标与“播放 / 继续观看”，接入本地选集和 mpv；首次由用户选择
  对应的媒体库作品，成功后记住关联，后续按真实文件进度续播。顶部横幅保留 seanime 的预览按钮
- Discover 根据原版实际交互重新对齐：使用 Embla 自由拖拽与惯性，逐张分页、圆点和类型筛选；
  海报悬停展开整卡详情和静音预告片，移出后收起。修正鼠标点击分页后自动轮播被焦点锁住的问题
- Discover 调整横幅尺寸、渐变、中文标题逐词淡入、预告片裁切与加载后延迟；分类使用滑动选中背景和
  内容进退场动画，窄屏隐藏横幅信息。页面独立加载，避免首页承担轮播和动画代码
- Schedule 按 seanime 重做为缺集横幅卡、七列月历与即将播出卡片；支持翻月、周首设置、
  状态筛选、已看标记、日期详情与背景图片轮换，窄屏改为逐日列表。Discover 放送标签使用近期日程
- Discover 补齐 seanime 动效：12 秒横幅轮播、退场暗化、封面缩放、文字分层淡入、
  指示条伸缩，以及海报行每 5 秒自动滑动；悬停、键盘焦点与暂停按钮会暂停轮播
- Discover 悬停作品信息 1 秒后加载静音 YouTube 预告片背景，确认播放后淡入，移开后停止；
  加载失败或超时保留图片。只开放 `youtube-nocookie.com` 嵌入，顶层脚本与 API 仍限同源
- 按 seanime v3.10.2（`9bdd052`）的实际组件重做导航、媒体库、发现、作品详情与设置页：
  80px 图标侧栏、顶部导航、大幅横图、海报网格、分类设置菜单；窄屏使用导航抽屉
- 发现页增加可切换、可暂停的精选横幅、横向翻页和作品预览弹窗；尊重减少动态效果设置，
  弹窗可用 Escape 关闭并恢复焦点。榜单、收藏和放送表均已连接真实接口
- 媒体库增加标题搜索、观看状态筛选和排序；详情页支持直接继续观看，剧集行显示文件名，
  并修正同时提供浏览器播放入口时的排列与进度条填充
- 设置入口支持直接定位播放器、账号、媒体文件夹、磁力引擎与规则来源；切换分区保留未提交输入
- 内置 Inter 可变字体和演示封面，统一图标、字号、控件尺寸；字体与演示图片均从本机加载
- 整套视觉换成媒体库应用的外观（参照 seanime）：近黑三层表面、发丝白描边、
  暗底上的状态色一律取浅档。原来那套青色 HUD 仪表盘语言连同它的 token 一起移除
- 导航收成左侧图标栏，三个页面不再各自重复渲染顶栏；「当前在哪一页」终于有了视觉表达
- **首次运行改成引导屏**：原来的空态只有一个「交出文件夹」表单，把一件可选的事
  摆成了唯一入口 —— 磁力搜索根本不需要任何本地文件夹。现在四个准备项摊开摆明，
  标清必需与可选，随时可以什么都不配就去用另一条路
- 根字号使用 100%，对应 seanime 的基准尺寸，同时尊重用户自己的浏览器字号设置
- 设置页的分区标题与字段名从小写英文改成中文
- **首页改成图像驱动**：顶部横幅 + 「继续观看」横向卡片行（封面、集号、
  已看进度），下方作品列表每部番带小海报。番剧封面本来就在 animego 的匹配
  响应里，M1 时被丢掉了，现在随匹配结果一起落盘 —— 凡是播放过的作品都有图，
  不额外发一轮请求（animego 全局限速 1 req/s）
- 封面经本机转发而不是让浏览器直连图床：CSP 的 `img-src 'self'` 不必放宽，
  用户 IP 不会在每次看库时送到图床，每部番只下载一次

## [0.2.0] — 2026-09-03

第一个公开版本。

### 本地媒体库

- 添加本地文件夹并扫描：噪声名过滤、1MB 体积下限、深度限制、ExFAT 包目录、NFC 归一
- 中文字幕组文件名解析：`【】` 括号剥离、全角转半角、集号/季号/字幕组/分辨率派生
- 剧集聚簇与分组；外挂字幕自动配对
- **懒哈希**：扫描阶段绝不读文件内容，首次播放前才算 dandanplay 匹配用的首 16MB 指纹
  —— 一千集导入不必读 16GB

### 播放

- 通过 JSON IPC 编排外部 mpv，不自己解码
- 弹幕转 ASS 字幕轨，**双轨编排**：对白保持主轨、弹幕挂副轨并保住 `\move` 样式；
  没有对白字幕时弹幕直接当主轨
- 观看进度节流回写，看完后同步账号
- 匹配或弹幕失败**不阻断播放**，失败原因在界面上可见

### 声明式源规则

- 规则用 YAML 描述「发一个 GET、按路径解字段、做几步固定转换」，**没有脚本、没有沙箱**
- 本体**零内置源**，规则仓库地址不预填，由用户自己提供
- 规则从用户指定的 HTTPS 仓库同步，校验后加载
- 搜索结果区分「无结果」与「源异常」——解出 0 字段不等于上游没有内容，
  站点改版会让规则静默失效，这两种状态必须分开
- `-validate-rules` 命令行开关：规则作者与规则仓库 CI 用的校验器，跑的就是引擎本身

### 磁力边下边播

- 磁力链接由用户提供，本体把 infohash 变成可拖进度条的本机视频流交给 mpv
- **复用同一条播放管线**：弹幕、进度、看完标记与本地文件走同一段代码
- 自动选集（复用文件名解析链）+ 手动兜底；花絮与非视频文件自动过滤
- 分阶段缓冲状态、实时分享者数与速度，随时可取消
- **停止播放即删分片**，缓存目录在启动与退出各清空一次，无容量上限
- 拒绝私有站（PT）种子并说明原因：BT 引擎无法只对单个种子关闭 DHT/PEX
- 关闭 webseed 与 WebTorrent：磁力 URI 的 `ws=` 参数会被当 webseed 执行

### 打包与分发

- macOS：ad-hoc 签名的 universal `.app` + dmg
- Windows：无签名 NSIS 安装包与便携版 zip，**内置 mpv**
- Linux：deb / rpm（`Depends: mpv`）/ tar.gz
- **零证书、零年费**：首次打开会有系统警告，按提示放行一次

### 更新

- 每天最多向 GitHub 查一次版本号，只发出版本号，可关闭
- **一键自更新**：minisign 验签 → sha256 校验 → 解包 → 原子替换 → 自动重启
- **防重放**：签名的可信注释必须写明要装的版本，否则一份签名合法的旧清单
  就能把用户降级回有洞的版本
- 包管理器装的（deb/rpm/Homebrew）不自更新，界面提示用 `apt`/`dnf`/`brew`
- 公钥内嵌在二进制里；**没有公钥的构建一律拒绝自更新**（失败关闭）
- `-update` 命令行开关供无界面场景使用，刻意不自动重启

### 安全

- 只监听 `127.0.0.1`；128 位随机 token（配置文件 0600）
- Host 头白名单挡 DNS rebinding；非 GET 必须把 token 放自定义头（挡 CSRF）
- 流端点走能力 URL，转发前剥掉能力段——它到不了处理器，也就进不了日志
- **不发送任何 CORS 头**
- 日志不记录请求 URL（GET 的 query 可能携带 token）

[0.2.0]: https://github.com/nagare-project/nagare/releases/tag/v0.2.0
