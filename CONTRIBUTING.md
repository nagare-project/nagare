# 为 nagare 做贡献

感谢你愿意参与。nagare 是一个通用的本地动漫播放 agent。在提交代码或功能请求之前，请先读完下面的红线——它们定义了项目的边界。

## 红线（不可谈判）

以下四条是项目边界。触碰任何一条的 PR 和功能请求都会被直接关闭，不进入讨论。

1. **源必须由用户提供，且只在用户自己的机器上执行。** nagare 本体不内置、不推荐、不精选任何内容源；任何“开箱即有源”的功能或 PR 都不会被接受。

   也就是说：安装包和默认配置里永远没有任何源。用户自己粘贴磁力链接、自己指定本地目录、自己安装源规则——获取和执行都只发生在用户自己的机器上。

2. **不向远程服务提供“按作品 ID 返回磁力/资源”的桥接接口。** nagare 是通用本地播放器，不为任何元数据网站充当资源解析后端，也不把来源能力暴露成远程服务。

   用户在 nagare 本机界面中主动选集后，可以把标题、集号和公开元数据标识交给用户自己安装的本地来源插件。这个例外只适用于回环地址上的进程间协议；插件不能获得 nagare/animego 登录凭据，nagare 的 HTTP API 仍受 Host、token 和 CSRF 边界保护。

3. **在线来源只能来自用户显式安装的本地插件。** nagare 不内置在线站点规则、域名、账号或绕过逻辑。插件必须在用户机器上运行，通过版本化声明式协议返回候选；安装、启用、搜索和播放都由用户主动触发，失败与换源必须可见且可取消。

   插件返回的短期 URL、Cookie 和请求头只存在于当前播放会话，不写入 Git、长期缓存或普通日志。插件进程必须只监听显式回环 IP，退出 nagare 时一并终止。

4. **保持独立。** nagare 不引用、不集成任何第三方网站的品牌、域名或账号体系；源规则仓库与本体分离，互不背书。

   这条同样约束文档、示例和默认配置。

## 开发环境

- Go 1.25+
- [bun](https://bun.sh)（前端包管理与构建）

构建与运行：

```bash
# 前端构建，产物输出到仓库根的 web/
cd frontend
bun install
bun run build

# 回到仓库根，构建单二进制
cd ..
go build

# 开发模式：前后端分开跑
go run . -no-browser          # 后端，不自动打开浏览器
cd frontend && bun run dev    # 前端 dev server，vite 把 /api 代理到 127.0.0.1:8590
```

## 提交规范

- 遵循 [Conventional Commits](https://www.conventionalcommits.org/)，类型使用：`feat:` / `fix:` / `refactor:` / `test:` / `chore:` / `ci:` / `docs:`
- **commit 的 subject 和 body 用英文**

示例：

```
feat: rotate stream capability URL on startup
```

## 代码规范

- **注释和文档用中文，代码标识符用英文**
- Go：`gofmt` + `go vet` 必须通过；测试用 `testing` + `testify`
- 前端：TypeScript strict 模式；测试用 `vitest`
- 单文件 200–400 行为宜，**800 行是硬上限**
- **错误不静默**：每一个新增的失败点都要想清楚两件事——用户看到什么、用户能做什么来恢复

## 测试要求

- 行为改动必须带测试，`go test ./...` 与 `cd frontend && bun run test` 全绿
- **碰 `internal/httpserver` 鉴权相关代码，必须补上对应攻击面的测试**，例如：
  - 不带 token 请求 API → `401`
  - 伪造的外来 `Host` 头 → `403`
  - 变更请求（非 GET）缺 `X-Nagare-Token` 自定义头 → `403`
  - 伪造的流端点能力 URL → `404`

鉴权是这个项目的安全地基，宁可测试写多，不接受“改动小就不用测”。

## PR 流程

1. Fork 本仓库，从最新主分支切出特性分支
2. 完成改动，确保本地测试全绿
3. 提交 PR，说明动机与改动内容
4. CI 会运行 `go test`、`vitest` 以及三平台交叉编译（darwin / linux / windows），全部通过后才会进入审阅

## 许可

本项目以 [AGPL-3.0](LICENSE) 授权。提交贡献即表示你同意你的贡献以 AGPL-3.0 授权发布。
