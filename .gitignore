# 内部指引 —— 含 animego 的业务数据与内部判断，不进公开仓库
CLAUDE.md

# 前端构建产物（go:embed 的落点，构建时生成）
# .gitkeep 必须进库：go:embed all:web 要求 web/ 目录存在且非空
/web/*
!/web/.gitkeep

# Node
node_modules/
frontend/dist/
.vite/
*.tsbuildinfo

# Go
/nagare
/nagare.exe
*.test
*.out
/dist/
/bin/

# 本地配置与运行时状态（含 token，绝不能进版本库）
/.nagare/
config.toml
config.local.*

# 下载目录 / 缓存
/downloads/
/cache/

# 编辑器与系统
.DS_Store
.idea/
.vscode/
*.swp

# 环境变量
.env
.env.*
!.env.example
.gstack/

# 调研用的浅克隆（seanime / animeko 等），不进仓库
scratchpad/

# 内部工程文档：评审记录、方案推演、自我更正。与 CLAUDE.md 同一性质 ——
# 里面写的是判断依据和踩过的坑，不是给使用者看的说明。
# docs/ 下其余文件是公开文档（releasing.md / rules-format.md 都被外部引用），
# 所以这里逐个列，不整目录忽略。
docs/scan-drops.md
docs/discover-lists-schedule.md

# 界面缺口清单。同上：里面是内部推演（哪条线不做、为什么），
# 不是对外说明。代码里的 FIXME(Gn) 编号仍然指得到它 —— 对维护者是活的，
# 对公开仓库不存在。
todos.md
